invoke>=2.2.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-bdd>=7.0
pytest-cov>=4.0
# TTH-08 Fase 7: e2e CT-08.11(e) levanta un Postgres/Timescale efímero para
# validar repo↔BD-real (mapping de columnas, idempotencia ON CONFLICT,
# CheckConstraints activos). Skip-graceful si Docker no está disponible.
testcontainers[postgres]>=4.0
